# Libre Note Editor Session Instructions

Before planning, editing, reviewing, or running implementation work in this repository, load these project documents into context:

1. `CONVENTIONS.md`
2. `DESIGN.md`

Treat `CONVENTIONS.md` as the source of truth for structure, naming, tests, linting, and quality rules.

Treat `DESIGN.md` as the source of truth for visual design, interaction, spacing, colors, layout, and UX decisions.

If a session is resumed after context compaction or a long interruption, re-check these files before continuing work.
